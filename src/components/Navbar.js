import React from 'react'


const Navbar = () => {

    const menuItems = [
        {name: `Home`, path: `/`},
        {name: `Portfolio`, path: `/portfolio`},
        {name: `Contact`, path: `/contact`}
    ];
  return (
    <div>
    </div>
  )
}

export default Navbar
